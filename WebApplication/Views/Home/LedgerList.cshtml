@using WebApplication.Models.ViewModel
@using WebApplication.Helper
@model IList<LedgerViewModel>
@{ 
    Layout = null;
}

<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>類別</th>
                    <th>日期</th>
                    <th>金額</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null)
                {
                    int index = 1;
                    foreach(var item in Model)
                    {
                        <tr>
                            <td>@(index++)</td>
                            <td>@item.Type.GetLedgerTypeName()</td>
                            <td>@item.Date.ToString("yyyy-MM-dd")</td>
                            <td>@item.Money.ToString("N0")</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>